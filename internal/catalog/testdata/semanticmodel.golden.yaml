# Generated by `ossiectl derive`. The spec.ossie block is an Apache Ossie
# semantic_model entry conforming to the Ossie core spec (v0.2.0.dev0):
#   https://github.com/apache/ossie/blob/main/core-spec/spec.md
# Physical structure (datasets, fields) is populated from the catalog;
# business semantics (metrics, relationships, synonyms, governance) are
# placeholders marked TODO for a human to fill. connection/governance/views
# are operator extensions kept outside the portable spec.ossie block.
apiVersion: "semantic.ossie.io/v1alpha1"
kind: SemanticModel
metadata:
  name: "derived-model"
  namespace: "semantic-system"
spec:
  connection:
    catalog: "glue"        # query-engine catalog fronting the lake
    database: "tpcds_3tb"
    viewDatabase: semantic_views   # use catalog.semantic_views on Trino
  ossie:
    name: "tpcds_3tb_model"
    description: ""                 # TODO: one line describing this model
    # ai_context guides LLM agents. Fill in and uncomment.
    # ai_context:
    #   instructions: >-
    #     TODO: what this model is for; call out any metric whose definition
    #     differs from naive per-row math (e.g. fan-out-safe ratios).
    datasets:
      - name: "date_dim"
        source: "date_dim"
        primary_key: []              # TODO: set PK column(s), e.g. ["d_date_sk"]; required for fan-out-safe ratio metrics
        # unique_keys:               # TODO: optional; other unique keys, each [col] or [col1, col2]
        # description: ""            # TODO: describe this dataset
        # ai_context:
        #   synonyms: []             # TODO: business names, e.g. ["sales", "transactions"]
        fields:
          - name: "d_date_sk"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "d_date_sk"
          - name: "d_date"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "d_date"
            dimension:
              is_time: true
      - name: "store"
        source: "store"
        primary_key: []              # TODO: set PK column(s), e.g. ["s_store_sk"]; required for fan-out-safe ratio metrics
        # unique_keys:               # TODO: optional; other unique keys, each [col] or [col1, col2]
        # description: ""            # TODO: describe this dataset
        # ai_context:
        #   synonyms: []             # TODO: business names, e.g. ["sales", "transactions"]
        fields:
          - name: "s_store_sk"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "s_store_sk"
          - name: "s_state"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "s_state"
            description: "two-letter: state code"
      - name: "store_sales"
        source: "store_sales"
        primary_key: []              # TODO: set PK column(s), e.g. ["ss_item_sk"]; required for fan-out-safe ratio metrics
        # unique_keys:               # TODO: optional; other unique keys, each [col] or [col1, col2]
        # description: ""            # TODO: describe this dataset
        # ai_context:
        #   synonyms: []             # TODO: business names, e.g. ["sales", "transactions"]
        fields:
          - name: "ss_item_sk"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "ss_item_sk"
          - name: "ss_store_sk"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "ss_store_sk"
          - name: "ss_sold_date_sk"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "ss_sold_date_sk"
          - name: "ss_ext_sales_price"
            expression:
              dialects:
                - dialect: ANSI_SQL
                  expression: "ss_ext_sales_price"

    # relationships: Glue/Iceberg do not record foreign keys. The edges below
    # were inferred from *_sk / *_id naming. Review each and move the correct
    # ones into the list (delete the leading '#').
    relationships: []
    #  - name: "store_sales_to_store"
    #    from: "store_sales"
    #    to: "store"
    #    from_columns: ["ss_store_sk"]
    #    to_columns: ["s_store_sk"]
    #    # "store_sales.ss_store_sk and store.s_store_sk share key suffix \"store_sk\""
    #  - name: "store_to_store_sales"
    #    from: "store"
    #    to: "store_sales"
    #    from_columns: ["s_store_sk"]
    #    to_columns: ["ss_store_sk"]
    #    # "store.s_store_sk and store_sales.ss_store_sk share key suffix \"store_sk\""

    # metrics: certified aggregate definitions, the core of the model. A model
    # with no metrics validates but answers nothing; define at least one. Example:
    metrics: []
    #  - name: total_sales
    #    expression:
    #      dialects:
    #        - dialect: ANSI_SQL
    #          expression: SUM(store_sales.ss_ext_sales_price)
    #    description: Total sales revenue
    #    ai_context:
    #      synonyms: ["revenue", "total sales", "gross sales"]

  # governance: compile-time row/column access policy. Uncomment and edit.
  # governance:
  #   defaultRole: analyst
  #   roles:
  #     - name: analyst
  #       allowMetrics: ["*"]
  #       denyFields: []                 # e.g. ["customer.c_email_address"]

  # views: governed BI views materialized in the query engine. Uncomment and edit.
  # views:
  #   - name: sales_by_category
  #     metrics: [total_sales]
  #     dimensions: [item.i_category]
  #     role: analyst

  # joins: per-relationship join type override (INNER default). Uncomment and edit.
  # joins:
  #   - relationship: <relationship_name>
  #     type: LEFT
