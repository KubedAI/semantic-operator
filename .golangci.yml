version: "2"

run:
  tests: true

linters:
  default: standard
  enable:
    - gocritic
    - misspell
    - unconvert
    - whitespace
  settings:
    staticcheck:
      checks:
        - all
        - -QF1001
        - -ST1000
  exclusions:
    generated: lax
    paths:
      # Vendored go-sql-driver/mysql (see mysqldriver/NOTICE).
      - internal/starrocks/mysqldriver/[^a].*\.go$
    rules:
      - path: 'api/v1alpha1/groupversion_info.go'
        linters:
          - staticcheck

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - internal/starrocks/mysqldriver/[^a].*\.go$
