# Third-party images pulled once and loaded into the kind node.
# One image ref per line; blank lines and lines starting with # are ignored.
# (confirm-on-pull): repo/tag not verifiable from the authoring sandbox — the
# first `make images-pull` will surface any wrong reference.

# --- Deployed via raw manifests (I control these refs) ---
dxflrs/garage:v2.3.0
apache/polaris:1.6.0
apache/polaris-admin-tool:1.6.0
postgres:16
# Apache Kafka (KRaft) — maintained upstream image; replaces the prereq chart's
# unmaintained bundled broker
apache/kafka:3.9.0

# --- StarRocks (kube-starrocks 1.11.6 chart; FE/CN pinned to 4.0.13) ---
starrocks/operator:v1.11.6
starrocks/fe-ubuntu:4.0.13
starrocks/cn-ubuntu:4.0.13

# --- Appended by `make charts-images` after `make charts-vendor` ---
# DataHub 1.0.3 + prerequisites
# 0.3.0 — enumerated by helm template on the vendored
# charts with local values. Do not hand-edit; re-run 'make charts-images'.
busybox:latest
docker.io/acryldata/datahub-frontend-react:v1.6.0
docker.io/acryldata/datahub-gms:v1.6.0
docker.io/acryldata/datahub-upgrade:v1.6.0
opensearchproject/opensearch:2.19.5
