{{- /*
  The external authorization providers used to be written to a mounted Secret.
  They are now rendered into the server config ConfigMap (see _helpers.tpl
  serverConfig / providerConfigs). Bearer tokens are still injected as env from
  their referenced Secrets by the server deployment. This file intentionally
  renders nothing.
*/ -}}
